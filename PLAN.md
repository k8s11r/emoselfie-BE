# 이모셀피 Backend 개발 계획

- 작성일: 2026-09-08
- 기준: [Backend spec v1.0](./spec.md), [PRD v1.0](../emoselfie-DOCS/requirements.md), [DECISIONS](../emoselfie-DOCS/DECISIONS.md), [Backend Guidelines](./guidelines.md)
- 실행 목록: [TODO.md](./TODO.md). FE 연동 기준: [Frontend 계획](../emoselfie-FE/PLAN.md), [Frontend 명세](../emoselfie-FE/spec.md).
- 현황: 2026-09-08 M0 기반과 세션·방 REST·실시간 대기실을 구현했다. 실제 2개 서버에서 소켓 갱신·중복 연결 교체를 포함해 66개 테스트가 통과했다. 개별 완료 범위는 TODO 및 [추적표](./docs/traceability.md)를 따른다. 실제 모델·라운드 진행·FE 브라우저 연동 완료를 의미하지 않는다.
- 충돌 시 PRD > DECISIONS > Guidelines를 따른다. BE spec의 D-1~D-8을 구현 기준으로 사용하되, 아래 미해결 계약을 임의로 확정한 것으로 취급하지 않는다.

## 1. 목표와 범위

서버가 방·참여자·라운드·제출 마감·채점·결과 접근 권한을 결정하고, 모바일 재접속 및 여러 Pod에서도 동일한 게임을 이어가는 백엔드를 만든다. 핵심 경로는 세션 → 방 입장 → 게임 시작 → 촬영 토큰 → 업로드 접수 → 비동기 채점 → 결과·리액션 → 다음 라운드 → 최종 순위다.

MVP는 2~12명, 3/5/7라운드(기본 5), 15/20/30초(기본 20), 전체 7감정, 순위 포인트 100/70/50/30을 제공한다. `no_face`는 30포인트 고정, `failed`는 보정 포인트, `missed`는 0포인트이며 리액션은 점수에 영향을 주지 않는다.

P0에 세션·방·참여·라운드·추론·결과·리액션·스킵·복원·접근제어·운영 검증을 포함한다. PRD상 P1인 자발적 나가기/재입장(RO-16), 1년 미접속 사용자 정리(ID-09), 난이도 세트/감정 배치 개선(EM-03·04)은 별도로 추적한다. 다만 PM-11의 권한 철회와 포인트 보존, disconnect 유예·퇴장은 P0다. RO-16은 BE 명세에 API가 있어 MVP 동반 구현 여부를 먼저 합의한다.

모델 학습, Gradio 서비스화, 별도 추론 마이크로서비스, 로그인/방장 비밀번호, 이미지 영구 저장, 공유 기능 활성화, 같은 방 다시하기는 이 계획의 구현 범위에 포함하지 않는다.

## 2. 구조와 데이터 책임

| 영역 | 구현 방향 |
|---|---|
| 실행 환경 | Guidelines의 Python 3.11, FastAPI, Uvicorn, Pydantic v2, python-socketio. 호환 의존성을 고정한 lock 파일과 CI 구성 |
| 모듈 | BE spec §2.2의 `api`, `realtime`, `domain`, `inference`, `media`, `db`, `core`. API/소켓 → domain → 저장소·추론. 채점은 순수 함수 |
| PostgreSQL | SQLAlchemy 2.x/asyncpg/Alembic. User·Room·Participant·Round·Submission·Reaction·RoundSkip 및 제약·복원 데이터 |
| Redis | 진행 중 제출·채점·리액션·스킵, presence, 연결 매핑, 잠금, 타이머, 레이트 리밋. 확정 포인트는 DB 트랜잭션으로 한 번 반영 |
| 사진 | 원본·crop은 처리 후 메모리 해제, 재인코딩한 결과 JPEG만 Redis 임시 캐시. 라운드/게임/방 종료 시 삭제, TTL 180초는 안전망 |
| 추론 | Protocol + FakeClassifier로 게임 개발 가능. 실제 모델은 시작 시 로드·warmup 후 ready. 운영에서 fake로 자동 대체하지 않음 |
| 시간·동시성 | UTC 저장, `now_ms()` 단일 경계, 외부 `...AtMs` 정수. DB 제약·상태 CAS·원자적 Redis 연산으로 중복 실행 방어 |
| 배포 | 동일 오리진의 `/api`, `/socket.io`, `/media` 분기. Pod당 Uvicorn 1프로세스·모델 1회 로드, 읽기 전용 모델 볼륨 |

인프라 협업 산출물은 [emoselfie-INFRA](../emoselfie-INFRA/README.md)와 연결한다. 이 단계에서는 배포를 실행하지 않고 이미지·환경변수·헬스체크·라우팅·모델 볼륨 계약과 검증 절차를 준비한다.

## 3. 구현 전 계약 게이트

아래는 검토에서 찾은 차이와 설계 제안이다. 명세 변경 확정 기록이 아니다. 공통 기반·순수 채점·fake 엔진은 먼저 만들 수 있지만, 관련 기능의 통합 완료는 계약·fixture 확정 후 판정한다.

| ID | 근거 및 확인할 문제 | 제안 또는 결정 산출물 | 필요 시점 |
|---|---|---|---|
| G-01 | §12·21-A: 얼굴 모델 및 `femo` 모듈 부재 | 모델 담당과 weight·아키텍처·라벨 순서·전처리·버전·체크섬·공급 경로 확정. 텍스트 모델을 대신 쓰지 않음 | M1 실제 추론 |
| G-02 | §7.3 재호출 안전과 §13.2 토글은 중복 전달 시 효과가 다름. §8.3 토큰 소비가 중복 응답보다 앞섬 | FE B-3·B-4: requestId/desired state 또는 중복 제거 계약, 동일 업로드 접수 조회·응답 유실 복구·오류 우선순위 합의 | M1 업로드, M2 명령 |
| G-03 | §8.3: body 검증 전 슬롯·토큰 소비와 viewers 승격. 느린 body, 413/415, 연결 중단의 후속 상태 미정 | 접수 예약/유효 이미지 인정 상태, rollback·재시도·body 시간 제한·scoring guard 관계 확정. 헤더 수신 시각 기준 유지. 잘못된 이미지로 결과 열람 불가 검증 | M1 업로드 |
| G-04 | §14의 snapshot 후 소켓 연결 사이 이벤트 유실, 뒤늦은 viewers 가입자의 이전 결과 누락 | FE B-1·B-2·B-5·B-11: screen별 DTO, revision/replay 또는 원자적 구독, backlog, processing/정렬/내 리액션·스킵 상태 fixture | M2 복원·결과 |
| G-05 | §6.4는 조회 3필드, §8.2는 isHost/settings 추가. 닉네임 규칙·참여자 추가·mutation DTO 불완전 | FE B-7·B-8·B-10: 입장 전 허용 필드, Unicode 길이/trim/문자 범위, 12색 재사용, HTTP·이벤트 응답 확정 | M2 입장 |
| G-06 | §3.2/8.2의 left 복귀 active와 진행 중 신규 waiting_next_game 구분, finished 이후 다음 게임 없음. CP-06의 카메라 실패 보정과 §6.4 일반 미제출 충돌 | FE Q-2·Q-3·Q-6/B-6·B-9: 기존 참여자·신규 입장·권한 철회·카메라 실패·finished 복원/이동 정책, 제출 후 퇴장 시 점수/권한 처리 확정 | M2 입장, M3 예외 |
| G-07 | §10.3/13.2: 스킵 허용 단계·미제출 방장의 권한·분모 0·이탈한 스킵 투표 처리 불명확. 제출 0건/마지막 voided도 명시 필요 | FE B-12, §21-B·C: 감상 시간·현재 connected viewers와 skips 교집합·방장 예외·무효/종료 경계표 확정 | M2 루프, M3 확정 |
| G-08 | §1 D-8은 finalized 때 Reaction 커밋, §13은 closed 전까지 리액션 허용. submitted ID도 DB 삽입 전 외부 노출 | Submission ID 할당·missed 저장 시점·finalized 점수 커밋·closed 최종 리액션 저장·RoundSkip 보존 정책 합의. 종료 후 투표 누락 및 포인트 중복 방지 | M2 데이터 확정 |
| G-09 | §10.4 ZREM 후 Pod 장애면 잡 유실 가능. DB 커밋 후 emit 실패, Redis 유실 복원도 미정 | 잡 lease/재시도 또는 DB 재조정, 멱등 CAS, 커밋 이벤트 재전달 설계. 전달 자체의 exactly-once 대신 상태 효과 1회 보장. 진행 라운드 손실 정책·tempHost 재구성 확정 | M2 설계, M3 장애 검증 |
| G-10 | §5의 DB 인덱스 분리만으로 Redis 메모리 정책 격리 가능 여부, §8.4는 채점 즉시 발급하지만 감상 종료 시각 미확정 | 조율/이미지 Redis 별도 인스턴스 구성 검토, 이미지 persistence/백업 제외, 미디어 초기 만료·갱신·권한 철회 계약. 프록시 body buffering/임시 디스크 기록도 확인 | M1 이미지, M5 운영 |
| G-11 | §11.2 정상자 평균과 §11.3 no_face 포함 평균, 반올림·완전 동점 처리 불명확 | 평균 모집단·반올림·동일 수신 ms 동점·0점 정상자와 no_face 정렬 예시 확정. 순수 함수 fixture로 고정 | M2 채점 |
| G-12 | §3.1 활동 트리거와 §13 presence:ping, §4.3 삭제 시점 표현 차이 | idle 기준, 종료/삭제 기준 시각 저장, 잡 취소·재예약, 소유 슬롯 즉시 반환/30분 보존, P1 정리 범위 합의 | M2 수명주기 |

FE Q-1(사진 삭제 고지)은 PV-01~03 및 D-1과 함께 정리한다. Q-7은 2026-09-08 확정된 기준을 그대로 적용한다. 결과 사진에 추가 반전·크롭을 하지 않으며, 추론용 얼굴 crop은 결과 이미지와 분리한다.

## 4. 단계별 실행 계획

M1~M5는 PRD §12의 마일스톤 의미를 유지한다. M0만 개발 준비 단계로 추가한다. 날짜는 담당 인원·모델 확보 시점이 미정이므로 고정하지 않는다.

| 단계 | 선행 조건 | 주요 작업 / 산출물 | 완료 기준 |
|---|---|---|---|
| M0 기반·계약 | 없음 | 패키지/모듈, 설정·시계·오류, 로컬 PG/Redis, migration, fake 엔진, 계약 결정표·fixture, CI | 빈 DB migration 및 fake 기반 unit/integration 실행 가능. 계약별 담당 역할·기한·상태 기록 |
| M1 기술 검증 | M0, G-01·02·03·10 | 실제 모델 로더·전처리·warmup, 헤더 시각 기반 제한 업로드, 비동기 러너, 캐시/서명 미디어. 격리된 개발 fixture의 단일 사용자 흐름 | 촬영 JPEG → 접수 → 판별 결과 전달·이미지 삭제 확인. 모델 누락 startup 실패, no_face/failed 구분, 실제 장비 benchmark |
| M2 실시간 게임 | M0 기반; 실제 모델 검증은 M1 | 세션·방·참여·소켓, 라운드 상태 머신·분산 타이머, 채점·DB 확정, 결과 권한·리액션·스킵·최종 순위, 초기 snapshot 복원 | 2명 이상이 실제 FE/BE로 3라운드를 완주. 미제출자 차단, 중복 포인트 없음, 종료 슬롯 반환. G-02~12 관련 계약 반영 |
| M3 예외·복구 | M2 | 60초 유예·방장 위임, 전 상태 복원, 중복 탭, 느린 업로드·권한 철회·무효 라운드, 2 Pod·의존성 장애 복구 | §18 규칙·다중 Pod 테스트 및 FE B-1~12 계약 테스트 통과. 10초 내 재접속 복귀와 사진 삭제 검증 |
| M4 비공개 베타 | M3, 실제 모바일 FE | 오프라인 모임 3회 이상, 오류·지연·완주 지표, no_face UX 및 기존 정책 관찰 | 실제 세션 증거·문제 목록·수정 검증 기록. fake 통과를 실제 모델/실기기 성공으로 대체하지 않음 |
| M5 공개 출시 준비 | M4 | 100방/1,200명 부하, 추론 동시성·메모리 튜닝, Docker/Infra 계약, readiness·종료·롤백·키 회전 runbook | 아래 품질 목표와 P0 추적표 통과. 운영 한도·배포/복원 절차 확정 |

M0 이후 모델 확보/추론 검증과 fake 기반 방·게임 도메인은 독립적으로 진행할 수 있다. 최종 M2 완료에는 실제 엔진 통합이 필요하다. 복원·권한·분산 멱등성 설계는 M2 전에 시작하며 M3에 검증을 확장한다.

## 5. 검증과 완료 기준

| 검증 축 | 필수 증거 |
|---|---|
| 순수 규칙 | §18.1 전체: 순위·동점·no_face·failed·missed·무효·최종 동점·mostLoved·리액션 점수 무관 |
| 경합 | 동시 방 생성/입장, deadline -1ms/정각/+1ms, 느린 body, 전원 제출 vs 타이머, 토글 재전달, finalize 재실행 |
| 권한 | 비참여자/대기자/미제출자/퇴장자의 REST·소켓·media 우회. 미제출자 결과 5개 이벤트 미수신, snapshot 정보 제거, 개인 토큰 교차 사용 거부 |
| 복구 | 모든 screen의 reload, 10초 재접속, 60초 이탈, 임시 방장 복귀, 동일 UUID 2탭, 작업 pop/추론/DB 커밋/emit 사이 Pod 중단 |
| 개인정보 | 원본/crop·UUID·토큰 로그 차단, Redis 이미지 persistence/백업 제외, 프록시·multipart 임시 디스크 점검, 모든 종료 경로 img 키 및 조회 폐기 |
| 성능 | 업로드 완료→클라이언트 결과 공개 p95 ≤3초, 클라이언트 상태 전환 편차 ≤500ms, 100방/1,200명. 추론 시간/잡 지연은 보조 지표이며 종단 측정을 대체하지 않음 |
| 운영 | 2개 ready Pod, 공통 Redis/PG, RO 모델 볼륨, startup 실패·의존성 ready 실패, graceful shutdown·재배포·복원 검증 |

변경마다 요구사항 ID·TODO ID를 기록하고 lint/format/typecheck 및 관련 unit/integration/contract test를 실행한다. DB·Redis의 원자성 검증은 실제 로컬 서비스로 수행한다. 스키마/페이로드 변경에는 migration 또는 JSON fixture를 함께 갱신한다.

전체 완료는 P0 체크리스트, §18 테스트, §19 요구사항 추적표, 실제 모델·모바일 연동, 부하 결과와 운영 절차가 모두 충족되었을 때 판정한다. P1 보류 및 합의한 제한은 릴리스 기록에 남긴다.

## 6. 첫 실행 순서

1. `BE-DEC-01~12`에 담당 역할과 결정 기한을 지정하고 FE B-1~12와 공통 fixture를 합의한다. 모델 확보 요청은 첫날 시작한다.
2. `BE-001~008`로 실행 환경·migration·fake 엔진·테스트 기반을 만든다.
3. `BE-010~018`로 실제 JPEG 업로드/추론 경로를 검증한다. 모델 대기 중에는 `BE-020~027`, `BE-040`의 독립 로직을 진행한다.
4. `BE-030~049`로 2인 완주 및 결과 접근제어를 완성하고 `BE-050~058`로 복구·장애를 검증한다.
5. `BE-060~068`로 실제 모바일 베타·부하·운영 검증을 마무리한다.

TODO의 S/M/L은 구현 상대 크기다. 모델 공급·계약 대기·베타 일정은 제외하며 L 항목은 구현 착수 시 PR 단위로 분할한다.
